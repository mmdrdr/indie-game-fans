<% if user_signed_in? %>
  <% if current_user.following?(user) %>
    <%= link_to "ファンです", user_relationships_path(user.id), method: :delete, remote: true, class: "nes-btn is-success" %>
  <% else %>
    <%= link_to "ファンになる", user_relationships_path(user.id), method: :POST, remote: true, class: "nes-btn is-primary" %>
  <% end %>
<% end %>